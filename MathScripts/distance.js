// calculating the distance

let x1 = 2;
let x2 = 4;
let y1 = 6;
let y2 = 10;

let distance = Math.sqrt((x2-x1)**2 + (y2-y2)**2);
console.log(`The distance between (${x1}, ${y1}) and (${x2}, ${y2}) is ${distance.toFixed(2)}`);